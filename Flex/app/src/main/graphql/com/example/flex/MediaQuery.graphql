query MediaQuery($id: Int) {
    media(id: $id) {
        id
        name
        path
        duration
        created
        status
        time_point
        last_seen
        width
        heigth
        size
        thumbnail
    }
}
